name: "Yamllint"

on:
  pull_request: {}

permissions:
  contents: read

jobs:
  cspell:
    name: Yamllint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: p6m7g8-actions/gh-repo-checkout@main

      - name: Run yamllint
        uses: ./
